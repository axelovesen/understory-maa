<!DOCTYPE html>
<html lang="no">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>

  <ul>
    <% companies.forEach(c => { %>
      <li><%= c.name %> - <%= c.score %></li>
    <% }) %>
  </ul>
</body>
</html>